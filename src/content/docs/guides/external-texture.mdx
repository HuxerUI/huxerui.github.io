---
title: ExternalTexture
description: Publish externally produced frames into HuxerUI image composition.
---

The shared `ExternalTexture` handle is platform-neutral; each backend exposes an `ExternalTextureSource` in its platform-specific header.
A source publishes frame data or a native texture according to that backend's ownership rules, then provides an `ExternalTexture` consumed by `Image` or paint commands.

## Choose a pixel integration

Choose an external texture for video, cameras, decoders, and GPU producers when the integration is pixel-oriented. Choose `PlatformView` when native interaction, accessibility, or platform view semantics must remain intact.

`Image(texture)` participates in normal HuxerUI fitting, clipping, opacity, transforms, and semantics.
The producer supplies pixels; HuxerUI continues to own layout, hit testing, and composition.

## Own source lifetime

Keep the source alive while frames are in use, publish only supported formats, and follow the adapter's threading rules. Intrinsic pixel dimensions and scale determine logical size. HuxerUI schedules repaint when the source publishes a new frame without requiring application-state recomposition.

Releasing the source ends publication, while copied `ExternalTexture` handles continue to identify the same shared state until all owners release it.
Do not rebuild application state for every frame.

The source and consumer have different ownership.
The platform-specific source owns writable publication state and synchronization; copied `ExternalTexture` values are read-only identities suitable for UI state, component arguments, and platform payloads.
Destroying one consumer does not stop a producer that still has other owners.

Publish only when the platform source reports that its publication contract is ready, and never mutate a buffer while the backend may still read it.
The platform header defines whether publication copies bytes, retains an object, or transfers a native resource.
Follow that rule rather than assuming all backends upload synchronously.

## Follow backend formats

Each platform header documents the accepted CPU frame or native texture representation, synchronization, and ownership transfer.
The shared API deliberately does not expose one platform's GPU handles to another backend.

Intrinsic pixel size and scale determine logical size; `Image` fitting then maps that logical content into layout bounds.
Use a stable intrinsic size for a video or camera stream unless a format change genuinely changes layout intent.
Frequent intrinsic-size changes can invalidate layout as well as paint.

Frame publication invalidates paint without recomposing application state.
Use ordinary state for playback controls, loading/error status, and accessibility labels, not for each frame number.

## Validate a producer

Verify first-frame behavior, resize/format change, hidden and clipped consumers, producer shutdown, source replacement, device/context loss where applicable, and publication from the documented thread.
Native GPU and media resources require device testing; a successful shared-header build does not validate synchronization or lifetime on another backend.

Use a `PlatformModule` when native code must control the producer or publish events in addition to frames.

See [ExternalTexture component](../../components/external-texture/) and the relevant platform API reference.
